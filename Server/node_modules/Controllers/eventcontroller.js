
var serverContext = require("./../../models/serverContext");


module.exports = {
    CreateEvent : function (newEvent) {
		var result = "OK";
		serverContext().then(function(models){
			models.user.create([newEvent], 
								function (error, items) {
									result = "Server error - " + error;
								});
		}, function(error) {
			result = "Server error - " + error;
		});
		return result;
    },

    RegisterUser: function (username, password, token, displayName) {
    }
};